import { ExtractCompanyPipe } from './extract-company.pipe';

describe('ExtractCompanyPipe', () => {
  it('create an instance', () => {
    const pipe = new ExtractCompanyPipe();
    expect(pipe).toBeTruthy();
  });
});
