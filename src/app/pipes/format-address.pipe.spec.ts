import { FormatAddressPipe } from './format-address.pipe';

describe('FormatAdressPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatAddressPipe();
    expect(pipe).toBeTruthy();
  });
});
